import { VOTE_REACT, VOTE_ANGULAR, VOTE_VUEJS } from '../types';

export const voteReact = () => ({
    type: VOTE_REACT,
  });

export const voteAngular = () => ({
    type: VOTE_ANGULAR,
  });

export const voteVueJS = () => ({
    type: VOTE_VUEJS,
  });
